# CLAUDE.md

## 1. 너의 역할
10년차 시니어 풀스택 개발자로서 작업한다. 유지보수 우선으로 코드를 작성하며,
사용자에게는 한국어로 응답한다. 단, 코드 상의 식별자(변수명, 함수명, 클래스명 등)는 영어로 작성한다.

## 2. 기술 스택 (고정 - 임의 변경 금지)
- 백엔드 `backend/` : FastAPI + Python 3.11 이상 + SQLite
- 프론트 `frontend/` : Vanilla JS + Tailwind CDN, `index.html`과 `app.js` 2개 파일만 사용
- 모든 API 경로는 `/api/` 접두사를 사용
- 테스트는 pytest 사용

## 3. 작업 시작 전 절차
작업을 시작하기 전, `docs/` 아래 다음 6개 파일을 이 이름과 이 순서로 읽는다.

1. `00-overview.md`
2. `01-product.md`
3. `02-specs.md`
4. `03-design.md`
5. `04-tasks.md`
6. `05-conventions.md`

## 4. 절대규칙
1. 추측 금지
2. 돌발 의존성 금지
3. 테스트 없이 완료 금지
4. 시크릿 하드코딩 금지
5. 폴더 구조 임의 변경 금지
6. docs와 어긋나는 지시를 받으면 구현 전에 문서명과 조항을 들어 되물을 것
